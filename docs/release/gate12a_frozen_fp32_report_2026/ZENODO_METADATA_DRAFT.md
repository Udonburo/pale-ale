# Gate12A Frozen FP32 Dense-Transformer Report: Zenodo Metadata Draft

Status: release-prep draft

## Recommended Upload Type

- `dataset`

Rationale:

- this deposit is best treated as a reproducible report bundle with software-side and evidence-side bindings
- the software repository remains separately available through Git and GitHub
- the key evidence and release packaging exceed what a GitHub tag alone would preserve

## Recommended Title

Primary:

```text
Gate12A Structural Replay Under a Frozen FP32 Dense-Transformer Regime: Closed 3B/4B Evidence, Transcript Extensions, and Boundary Results
```

Shorter:

```text
Gate12A Structural Replay Under a Frozen FP32 Dense-Transformer Regime
```

Conservative:

```text
Scoped Gate12A Report Bundle for a Frozen FP32 Dense-Transformer Replay Regime
```

## Abstract Draft

```text
This deposit records a scoped report bundle for the current Gate12A frozen FP32 dense-transformer line in pale-ale.

The release is intentionally narrow. It does not claim a universal law for all language models, a final theory of reasoning, or unchanged extension beyond the current frozen dense-transformer regime. Instead, it fixes a report-facing empirical surface consisting of the current paper PDF, the paper source, release-side provenance and integrity material, a frozen implementation snapshot, and the evidence bundle or stable evidence links associated with the report.

Within that scoped surface, the report records the following narrow result. Under the frozen FP32 dense-transformer regime, a Gate12A structural replay signature persists across the closed 3B/4B full-family-set lines. The phenotype layer remains family-conditioned and model-specific, with the clearest limited convergence appearing at archive high tension. Transcript-only observations broaden the structural range without being treated as mainline phenotype evidence, and boundary results record both frozen-protocol admission failures and a non-transformer sidecar that does not preserve the current dense-transformer transcript-path signature under an altered observation surface.

The purpose of this deposit is to preserve a timestamped report surface that can be inspected, reproduced, challenged, or extended without collapsing the software-side reference point, the artifact-side evidence, and the paper-facing release packaging into one ambiguous object.
```

## Description Notes

Keep the description aligned with these limits:

- this is a scoped report bundle
- this is about the current Gate12A frozen FP32 dense-transformer line
- transcript-only extensions are supporting structural evidence, not the same claim surface as the closed 3B/4B family-set lines
- boundary results delimit the regime rather than expanding it

Do not claim:

- universal law
- final theory
- unrestricted architectural portability
- family-independent phenotype invariance

## Related Identifiers To Add

Once final:

1. GitHub repository URL
2. GitHub release URL for the tagged software-side freeze
3. Git tag URL for the exact tagged commit
4. optional second Zenodo artifact-record DOI if the evidence bundle is split out

## Keywords

Recommended starting set:

- `LLM auditing`
- `structural replay`
- `discrete connection`
- `holonomy`
- `closure tension`
- `dense transformers`
- `frozen regime`
- `reproducible research`
- `boundary results`

## Creator Note

Fill creator fields exactly as desired for the public Zenodo record after the final publication decision is made.
