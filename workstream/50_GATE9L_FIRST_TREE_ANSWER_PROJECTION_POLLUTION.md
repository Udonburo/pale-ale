# Gate9L First-Tree Answer-Projection Pollution

Status: narrow blocker-audit spec, first implementation landed and first smoke execution recorded
Role: Gate9L first-tree residual-pollution spec, not decomposition verdict or operator opening
Date: 2026-03-21

Gate9L proceeds from:

- `48_GATE9K_TRUSTED_TREE_RESIDUAL_CHORD_DECOMPOSITION.md`
- `49_GATE9K_TRUSTED_TREE_RESIDUAL_CHORD_LOGGING_SMOKE.md`

The first Gate9L first-tree consumer now exists in:

- `tools/run_gate9l_first_tree_answer_projection_pollution_audit.py`

The first tracked Gate9L smoke execution read is now recorded in:

- `51_GATE9L_FIRST_TREE_ANSWER_PROJECTION_POLLUTION_SMOKE.md`

The next cleaner-side pollution anatomy spec is now recorded in:

- `52_GATE9M_CLEANER_ANSWER_PROJECTION_POLLUTION.md`

The first tracked Gate9M smoke execution read is now recorded in:

- `53_GATE9M_CLEANER_ANSWER_PROJECTION_POLLUTION_SMOKE.md`

## 0. Why This Exists

Gate9K made the decomposition policy executable as logging.

What is now known is:

- trusted-edge policy is explicit
- residual chord policy is explicit
- doctrine guards are still intact
- decomposition itself has not yet been executed

The next honest move is therefore not:

- a full residual verdict
- tree-choice sensitivity study
- operator promotion

It is:

- building one deterministic first tree or forest under the declared trusted-edge policy
- naming whether cleaner-side `answer_projection` edges are the active residual pollution source

## 1. Scope

Gate9L studies only:

- first deterministic tree or forest construction under the Gate9K trusted-edge policy
- residual chord availability after that construction
- whether cleaner-side residual pollution is concentrated in `answer_projection`

It does not:

- execute tree-choice sensitivity
- settle the trusted-tree hypothesis
- reopen operator admission

## 2. Public Question

The Gate9L question is:

- under one deterministic first tree construction, does `answer_projection` remain as cleaner-side residual chord pollution that blocks an honest bypass read

More concretely:

- does the trusted-edge policy already form a cycle-free forest
- do cleaner cells retain residual chords only through `answer_projection`
- do conflict cells retain anomaly-side residual bridges beyond that cleaner residue

## 3. Public Object

Gate9L must emit:

- a first-tree edge registry
- residual chord pollution summaries by cell
- a deterministic blocker status payload

The public object is not a bypass score.

It is:

- a first-tree residual-pollution audit

## 4. Gate9L Falsifiers

Gate9L must keep these falsifiers explicit:

- the trusted-edge policy does not build a clean first forest
- cleaner-side residual pollution is not actually concentrated in `answer_projection`
- conflict-side residual bridges are not distinguishable from cleaner-side residue

If those fail, the next blocker is not yet properly named.

## 5. What This Audit Can Earn

At most, Gate9L can earn the right to say:

- a deterministic first tree can be built
- the active residual pollution source after that build is named

It still does not earn:

- trusted-tree success
- blocker resolution
- operator opening

## 6. Current Memory Hook

The shortest acceptable sentence is:

- build one tree first, then name the residual pollution
