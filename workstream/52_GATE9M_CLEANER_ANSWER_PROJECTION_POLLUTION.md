# Gate9M Cleaner Answer-Projection Pollution

Status: pollution anatomy spec, first implementation landed and first smoke execution recorded
Role: Gate9M cleaner-side answer-projection pollution spec, not decomposition verdict or operator opening
Date: 2026-03-21

Gate9M proceeds from:

- `50_GATE9L_FIRST_TREE_ANSWER_PROJECTION_POLLUTION.md`
- `51_GATE9L_FIRST_TREE_ANSWER_PROJECTION_POLLUTION_SMOKE.md`

The first Gate9M pollution-audit consumer now exists in:

- `tools/run_gate9m_cleaner_answer_projection_pollution_audit.py`

The first tracked Gate9M smoke execution read is now recorded in:

- `53_GATE9M_CLEANER_ANSWER_PROJECTION_POLLUTION_SMOKE.md`

The next cleaner-side role-coupling spec is now recorded in:

- `54_GATE9N_CLEANER_ANSWER_PROJECTION_ROLE_COUPLING.md`

## 0. Why This Exists

Gate9L resolved one important ambiguity.

What is now known is:

- a deterministic first trusted forest can be built
- conflict-side residual bridges remain present
- bypass readiness is still denied
- the active named blocker is `cleaner_answer_projection_residual_pollution`

So the next honest move is not:

- tree-choice sensitivity
- operator opening
- a bypass success claim

It is:

- decomposing the cleaner-side `answer_projection` pollution itself

## 1. Scope

Gate9M studies only:

- cleaner-side `answer_projection` residual chords
- their role after the Gate9L first forest is fixed
- whether that pollution is structural closure residue or policy-mixing residue

It does not:

- change the trusted-edge policy
- change the first forest
- reopen operator admission
- execute tree-choice sensitivity

## 2. Fixed Conditions

Gate9M keeps all of the following fixed:

- trusted-edge policy fixed
- first forest fixed
- residual chord policy fixed
- operator admission still denied

This matters because Gate9M is not allowed to blur a pollution diagnosis into a policy rewrite.

## 3. Public Question

The Gate9M question is:

- `is cleaner-side answer_projection pollution structural closure residue or policy-mixing residue?`

This question is intentionally narrow.

It does not ask whether `answer_projection` is good or bad in general.

It asks why it remains in the cleaner-side residual set under the declared Gate9K/Gate9L setup.

## 4. Public Split

Gate9M must keep the split explicit between:

- `structural_return_leg_pollution`
- `policy_mixing_pollution`

The first means:

- cleaner-side `answer_projection` remains because it is the declared return leg required to preserve closure doctrine

The second means:

- cleaner-side `answer_projection` remains because the current residual-role policy mixes closure-maintaining return legs with anomaly-bearing residual chords under one residual headline

Gate9M does not assume in advance which branch wins.

It fixes the split so the next audit can falsify one against the other.

## 5. Falsifiers

Gate9M must keep these falsifiers explicit:

- cleaner-side pollution disappears under branch split
- conflict-side residual bridge degrades the same way
- removing cleaner-side `answer_projection` would break declared closure doctrine
- suppression requires scalar masking or undeclared role surgery

If those fail, the pollution anatomy is not yet properly named.

## 6. What This Spec Can Earn

At most, Gate9M can earn the right to say:

- cleaner-side `answer_projection` pollution is now decomposable into a structural versus policy-mixing question under fixed doctrine

It still does not earn:

- blocker resolution
- trusted-tree success
- operator opening

## 7. Current Memory Hook

The shortest acceptable sentence is:

- split cleaner answer-projection pollution before trying to suppress it
