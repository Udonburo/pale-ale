# Gate9O Declared Split Adoption-Worthiness

Status: spec-only, first implementation landed and first smoke execution recorded
Role: Gate9O declared-split adoption-worthiness spec, not mainline adoption or operator opening
Date: 2026-03-21

Gate9O proceeds from:

- `54_GATE9N_CLEANER_ANSWER_PROJECTION_ROLE_COUPLING.md`
- `55_GATE9N_CLEANER_ANSWER_PROJECTION_ROLE_COUPLING_SMOKE.md`

The first Gate9O adoption-worthiness consumer now exists in:

- `tools/run_gate9o_declared_split_adoption_worthiness_audit.py`

The first tracked Gate9O smoke execution read is now recorded in:

- `57_GATE9O_DECLARED_SPLIT_ADOPTION_WORTHINESS_SMOKE.md`

The next declared-split adopt-or-defer judgment spec is now recorded in:

- `58_GATE9P_DECLARED_SPLIT_ADOPT_OR_DEFER_JUDGMENT.md`

## 0. Why This Exists

Gate9N earned one narrow result.

What is now known is:

- cleaner-side `answer_projection` role coupling is declaratively separable under the frozen audit split
- conflict-side bridge preservation remains clear under that split
- closure doctrine remains clear under that split
- scalar masking and undeclared role surgery remain denied

What is not yet known is:

- whether that separable split is worth adopting under the frozen Gate9 doctrine

So the next honest move is not:

- declare mainline adoption
- reopen operator admission
- rename the result as blocker resolution

It is:

- auditing whether the declared split is adoption-worthy

## 1. Scope

Gate9O studies only:

- the baseline Gate9L/Gate9M residual policy
- the declared Gate9N split between `closure_return_leg_auxiliary` and `residual_chord_candidate`
- whether the declared split is decision-relevant enough to be adoption-worthy under the frozen doctrine

It does not:

- change the trusted-edge policy
- change the first forest
- change the cycle family
- reopen operator admission
- introduce new metrics or new public roles

## 2. Fixed Conditions

Gate9O keeps all of the following fixed:

- trusted-edge policy fixed
- first forest fixed
- closure doctrine fixed
- operator admission still denied

This matters because Gate9O is not allowed to turn an adoption audit into a doctrine rewrite.

## 3. Public Question

The Gate9O question is:

- `is the Gate9N declared split adoption-worthy under the frozen Gate9 doctrine?`

This question is intentionally narrow.

It does not ask whether the entire trusted-tree line has already succeeded.

It asks whether the split that Gate9N showed to be separable earns the right to be treated as a candidate adoption line rather than a mere audit artifact.

## 4. Public Comparison

Gate9O must compare only:

- baseline: the Gate9L / Gate9M residual-role policy
- declared split: the Gate9N role split

The comparison must remain decision-shaped rather than score-shaped.

The central question is:

- does the declared split reduce cleaner-side residual pollution in a bypass-relevant way while preserving the conflict-side bridge and closure doctrine

## 5. Source Run

The frozen source run for Gate9O is:

- `runs/gate9n_cleaner_answer_projection_role_coupling_smoke_from_gate9m`

If that source run is unavailable or inconsistent with the current spec, implementation must stop at gap reporting.

## 6. Expected Output Files

Gate9O implementation should emit exactly these first-class outputs:

- `manifest.json`
- `declared_split_adoption_worthiness_registry.jsonl`
- `declared_split_adoption_worthiness_policy_compare.csv`
- `declared_split_adoption_worthiness_status.json`
- `gate9o_declared_split_adoption_worthiness_read.md`

Additional helper files are allowed only if they do not change the public object.

## 7. Required Status Keys

The Gate9O status payload must emit at least:

- `baseline_bypass_readiness_status`
- `declared_split_bypass_readiness_status`
- `conflict_bridge_preservation_status`
- `closure_doctrine_preservation_status`
- `cleaner_pollution_reduction_status`
- `decision_relevant_cleaner_pollution_reduction_status`
- `adoption_worthiness_status`
- `operator_admission_non_promotion_status`
- `scalar_masking_violation_status`
- `next_named_blocker`

These keys are required so adoption-worthiness remains comparable to the earlier blocker line and does not drift into free-form prose.

## 8. Falsifiers

Gate9O must keep these falsifiers explicit:

- declared split leaves bypass readiness denied for the same reason
- conflict-side bridge degrades
- closure doctrine breaks
- cleaner-side residual pollution does not reduce in a decision-relevant way
- adoption requires scalar masking or undeclared role surgery

If those falsifiers land, the split has not earned adoption-worthiness.

## 9. Forbidden

The Gate9O implementation must not:

- declare mainline adoption
- reopen operator admission
- rename blockers
- rename falsifiers
- widen scope
- introduce new metrics
- introduce new public roles
- change doctrine

If the spec appears insufficient, implementation must:

- emit the gap clearly
- stop without filling it by invention

## 10. What This Spec Can Earn

At most, Gate9O can earn the right to say:

- the declared split is adoption-worthy

or:

- the declared split is not yet adoption-worthy, with one next named blocker

It still does not earn:

- automatic mainline adoption
- operator opening
- trusted-tree success in general
- a new geometry claim

## 11. Opus-Safe Delegation Boundary

Gate9O is safe to delegate only as:

- narrow consumer implementation
- unit tests
- status payload emission
- tracked smoke handoff formatting

Gate9O is not delegated as:

- blocker naming
- falsifier design
- scope narrowing
- doctrine change
- next workstream judgment

## 12. Current Memory Hook

The shortest acceptable sentence is:

- test whether the separable split is worth adopting before treating it as mainline
