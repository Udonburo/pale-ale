# Gate12A Qwen3-4B Fixed Family Set Replication Memo

Status: empirical model memo draft
Role: scoped dense-transformer family-set empirical memo over the current Gate12A observable surface, recording one completed `Qwen/Qwen3-4B` fixed-family-set replay plus attached local provisional first-pass reads, not a universal law claim, not a checkpoint rewrite, and not a Gate12B promotion
Date: 2026-04-04

This memo proceeds from:

- `195_GATE11_ADMISSIBILITY_MEMBRANE_FREEZE.md`
- `196_GATE12_DISCRETE_CONNECTION_CONSTITUTION.md`
- `197_GATE12A_DISCRETE_CONNECTION_IMPLEMENTATION_CONTRACT.md`
- `198_GATE12A_DISCRETE_CONNECTION_AUDIT.md`
- `200_GATE12A_TRANSCRIPT_V1_ANCHOR_RICH_CLOSURE_TENSION_REPLICATION_MEMO.md`
- `201_GATE12A_BRIEFING_V1_ANCHOR_RICH_CLOSURE_TENSION_EMPIRICAL_MEMO.md`
- `202_GATE12A_TRANSCRIPT_V1_VS_BRIEFING_V1_ANCHOR_RICH_CLOSURE_TENSION_COMPARISON_MEMO.md`
- `203_GATE12A_ARCHIVE_V1_ANCHOR_RICH_CLOSURE_TENSION_FIRST_PASS_BREAK_CANDIDATE_MEMO.md`
- `210_GATE12A_QWEN_2_5_3B_INSTRUCT_FIXED_FAMILY_SET_REPLICATION_MEMO.md`

## 0. Scope

This memo asks only:

- `what stayed fixed, and what shifted, when Qwen/Qwen3-4B was taken through the current transcript_v1 / briefing_v1 / archive_v1 family set under the unchanged Gate12A observable surface?`

Its subject is fixed to:

- `Qwen/Qwen3-4B`
- `transcript_v1 / briefing_v1 / archive_v1`
- `current committed Gate12A cross-model replay harness`
- `current committed first-pass capture layer`

It does:

- keep the Gate12A observable unchanged
- keep the model fixed to one dense-transformer line
- close the current `Qwen3-4B` family set at machine scope
- attach one local provisional first-pass read to each family packet
- compare the resulting first-pass family set against the current `Qwen/Qwen2.5-3B-Instruct` line

It does not:

- claim a transformer-universal law
- claim that all phenotype behavior is now model-invariant
- promote a new threshold doctrine
- revise the published checkpoint boundary beyond `202`

## 1. Evidence Base

This memo is limited to one current local run surface:

- `runs/gate12a_cross_model_replay_qwen_qwen3_4b/`
- `runs/gate8cm_qwen_qwen3_4b_transcript_128r_candidate_execution/`
- `runs/gate8cm_qwen_qwen3_4b_briefing_200r_candidate_execution/`
- `runs/gate8cm_qwen_qwen3_4b_archive_128r_candidate_execution/`
- `runs/gate12a_triangle_phenotype_first_pass_recheck_from_gate12a_upstream_gate8cm_qwen_qwen3_4b_transcript_128r_gate9k/`
- `runs/gate12a_triangle_phenotype_first_pass_recheck_from_gate12a_upstream_gate8cm_qwen_qwen3_4b_briefing_200r_gate9k/`
- `runs/gate12a_triangle_phenotype_first_pass_recheck_from_gate12a_upstream_gate8cm_qwen_qwen3_4b_archive_128r_gate9k/`

The current cross-model replay summary records:

- `run_id = gate12a_cross_model_replay_qwen_qwen3_4b`
- `code_git_commit = 084eb7878d8cb016243950e1cf4b4bd7379daaba`
- `family_set = transcript_v1 / briefing_v1 / archive_v1`
- `summarize_only = true`

So this memo concerns:

- one completed `Qwen3-4B` fixed family set under the current committed protocol

and not:

- an arbitrary cross-model sweep

## 2. Structural Replay Held Across The Full Qwen3-4B Family Set

Across all three current `Qwen/Qwen3-4B` families, the same machine-side structural checkpoints stayed live:

- `zero_overlap_clear = True`
- `all_defined_triangles_anchor_rich = True`
- `trusted_tree_gt_residual_chord = True`
- `plain_gt_anchor_qualified = True`

The current medians are:

- transcript_v1
  - `trusted_tree = 0.976138`
  - `residual_chord = 0.397724`
  - `anchor_qualified = 0.463775`
  - `plain = 0.981493`
- briefing_v1
  - `trusted_tree = 1.008876`
  - `residual_chord = 0.487303`
  - `anchor_qualified = 0.558445`
  - `plain = 1.017944`
- archive_v1
  - `trusted_tree = 0.988239`
  - `residual_chord = 0.394773`
  - `anchor_qualified = 0.448280`
  - `plain = 0.995090`

So this memo earns the narrow machine-side sentence:

- the current Gate12A structural replay survives across the full current `Qwen/Qwen3-4B` family set

## 3. First-Pass Family Surface On Qwen3-4B

The attached local provisional first-pass captures are:

- transcript_v1
  - `high_tension: surface_noise_only = 4; support_fused = 2`
  - `flat: surface_noise_only = 6`
- briefing_v1
  - `high_tension: surface_noise_only = 4; conflict_respected = 2`
  - `flat: anchor_overreach = 2; conflict_respected = 2; surface_noise_only = 2`
- archive_v1
  - `high_tension: conflict_adopted_break_candidate = 6`
  - `flat: surface_noise_only = 4; support_fused = 2`

This means the current `Qwen/Qwen3-4B` family set does not collapse to one phenotype rule.
It preserves distinct family-conditioned first-pass behavior under one fixed model.

## 4. Comparison Against Existing Qwen Lines

### 4.1 Transcript shifts away from the Qwen2.5-3B transcript line

The current `Qwen/Qwen3-4B` transcript split does not match the current `Qwen/Qwen2.5-3B-Instruct` transcript split:

- current `Qwen/Qwen3-4B` transcript
  - `high_tension: surface_noise_only = 4; support_fused = 2`
  - `flat: surface_noise_only = 6`
- current `Qwen/Qwen2.5-3B-Instruct` transcript
  - `high_tension: conflict_respected = 4; hypothetical_rejection = 2`
  - `flat: conflict_respected = 4; surface_noise_only = 2`

So the vendor-upgrade probe does not simply preserve the current `Qwen2.5-3B` transcript shape.
It softens toward a surface-noise-heavy read and drops the prior flat-band conflict-respected majority.

### 4.2 Briefing keeps the Qwen2.5-3B flat split but shifts the high-tension packet

The current `Qwen/Qwen3-4B` briefing flat split matches the current `Qwen/Qwen2.5-3B-Instruct` flat split:

- `anchor_overreach = 2; conflict_respected = 2; surface_noise_only = 2`

But the current `Qwen/Qwen3-4B` high-tension split does not match the current `Qwen/Qwen2.5-3B-Instruct` high-tension split:

- current `Qwen/Qwen3-4B` briefing high_tension
  - `surface_noise_only = 4; conflict_respected = 2`
- current `Qwen/Qwen2.5-3B-Instruct` briefing high_tension
  - `support_fused = 4; conflict_respected = 2`

So the current `Qwen/Qwen3-4B` briefing line preserves the same flat-band minority structure while shifting high-tension away from `support_fused` and toward `surface_noise_only`.

### 4.3 Archive hardens the break-bearing high-tension packet

The current `Qwen/Qwen3-4B` archive line is more severe at high tension than the current `Qwen/Qwen2.5-3B-Instruct` archive line:

- current `Qwen/Qwen3-4B` archive
  - `high_tension: conflict_adopted_break_candidate = 6`
  - `flat: surface_noise_only = 4; support_fused = 2`
- current `Qwen/Qwen2.5-3B-Instruct` archive
  - `high_tension: conflict_adopted_break_candidate = 4; surface_noise_only = 2`
  - `flat: compensatory_closure = 2; conflict_respected = 2; surface_noise_only = 2`

So the current `Qwen/Qwen3-4B` archive line returns to a total high-tension break-candidate packet while shifting the flat minority from `compensatory_closure/conflict_respected` toward `support_fused`.

## 5. What This Memo Earns

This memo earns only the following narrow sentence:

- `Qwen/Qwen3-4B closes the current Gate12A family set at machine scope, but its vendor-upgrade first-pass surface does not simply inherit the Qwen2.5-3B line: transcript shifts toward surface_noise_only and support_fused, briefing preserves the Qwen2.5 flat split while moving high_tension away from support_fused, and archive hardens back to a total high-tension break-candidate packet with a support_fused flat minority.`

This is enough to say:

- one newer Qwen line closes the current family set structurally
- transcript, briefing, and archive all remain family-conditioned at first pass
- vendor-upgrade portability stays narrower than machine-side structural portability

This is not enough to say:

- that all Qwen family upgrades preserve one stable phenotype law
- that briefing_v1 has stabilized across the Qwen line
- that archive_v1 now has one fixed flat-band sentence
- that `202` should be replaced as the published checkpoint
- that a Zenodo-bound release tag is now warranted

## 6. Release Boundary Note

This memo should be treated as:

- a tracked post-checkpoint follow-up memo

and not as:

- a new release tag boundary

The current published checkpoint remains `202`.
The memo still depends on local provisional first-pass attachments under `runs/`.
So it belongs in:

- git history

but not yet in:

- a new Zenodo-triggering release tag
