# Gate12A Llama-3.2-1B-Instruct Fixed Family Set Replication Memo

Status: empirical model memo draft
Role: scoped dense-transformer family-set empirical memo over the current Gate12A observable surface, recording one completed Llama fixed-family-set replay plus attached local provisional first-pass reads, not a universal law claim, not a checkpoint rewrite, and not a Gate12B promotion
Date: 2026-04-02

This memo proceeds from:

- `195_GATE11_ADMISSIBILITY_MEMBRANE_FREEZE.md`
- `196_GATE12_DISCRETE_CONNECTION_CONSTITUTION.md`
- `197_GATE12A_DISCRETE_CONNECTION_IMPLEMENTATION_CONTRACT.md`
- `198_GATE12A_DISCRETE_CONNECTION_AUDIT.md`
- `200_GATE12A_TRANSCRIPT_V1_ANCHOR_RICH_CLOSURE_TENSION_REPLICATION_MEMO.md`
- `201_GATE12A_BRIEFING_V1_ANCHOR_RICH_CLOSURE_TENSION_EMPIRICAL_MEMO.md`
- `202_GATE12A_TRANSCRIPT_V1_VS_BRIEFING_V1_ANCHOR_RICH_CLOSURE_TENSION_COMPARISON_MEMO.md`
- `203_GATE12A_ARCHIVE_V1_ANCHOR_RICH_CLOSURE_TENSION_FIRST_PASS_BREAK_CANDIDATE_MEMO.md`
- `205_GATE12A_LLAMA_3_2_1B_INSTRUCT_TRANSCRIPT_V1_REPLICATION_MEMO.md`

## 0. Scope

This memo asks only:

- `what stayed fixed, and what shifted, when meta-llama/Llama-3.2-1B-Instruct was taken through the current transcript_v1 / briefing_v1 / archive_v1 family set under the unchanged Gate12A observable surface?`

Its subject is fixed to:

- `meta-llama/Llama-3.2-1B-Instruct`
- `transcript_v1 / briefing_v1 / archive_v1`
- `current committed Gate12A cross-model replay harness`
- `current committed first-pass capture layer`

It does:

- keep the Gate12A observable unchanged
- keep the model fixed to one dense-transformer line
- close the current Llama family set at machine scope
- attach one local provisional first-pass read to each family packet
- compare the resulting first-pass family set against the current Qwen and SmolLM2 lines

It does not:

- claim a transformer-universal law
- claim that all phenotype behavior is now model-invariant
- promote a new threshold doctrine
- revise the published checkpoint boundary beyond `202`

## 1. Evidence Base

This memo is limited to one current local run surface:

- `runs/gate12a_cross_model_replay_meta_llama_llama_3_2_1b_instruct/`
- `runs/gate8cm_meta_llama_llama_3_2_1b_instruct_transcript_128r_candidate_execution/`
- `runs/gate8cm_meta_llama_llama_3_2_1b_instruct_briefing_200r_candidate_execution/`
- `runs/gate8cm_meta_llama_llama_3_2_1b_instruct_archive_128r_candidate_execution/`
- `runs/gate12a_triangle_phenotype_first_pass_recheck_from_gate12a_upstream_gate8cm_meta_llama_llama_3_2_1b_instruct_transcript_128r_gate9k/`
- `runs/gate12a_triangle_phenotype_first_pass_recheck_from_gate12a_upstream_gate8cm_meta_llama_llama_3_2_1b_instruct_briefing_200r_gate9k/`
- `runs/gate12a_triangle_phenotype_first_pass_recheck_from_gate12a_upstream_gate8cm_meta_llama_llama_3_2_1b_instruct_archive_128r_gate9k/`

The current cross-model replay summary records:

- `run_id = gate12a_cross_model_replay_meta_llama_llama_3_2_1b_instruct`
- `code_git_commit = aa84eef2f72c0be26854e2289fde5aa4b8aeefc6`
- `family_set = transcript_v1 / briefing_v1 / archive_v1`
- `summarize_only = true`

So this memo concerns:

- one completed Llama fixed family set under the current committed protocol

and not:

- an arbitrary cross-model sweep

## 2. Structural Replay Held Across The Full Llama Family Set

Across all three current Llama families, the same machine-side structural checkpoints stayed live:

- `zero_overlap_clear = True`
- `all_defined_triangles_anchor_rich = True`
- `trusted_tree_gt_residual_chord = True`
- `plain_gt_anchor_qualified = True`

The current medians are:

- transcript_v1
  - `trusted_tree = 0.964378`
  - `residual_chord = 0.491321`
  - `anchor_qualified = 0.459245`
  - `plain = 0.979434`
- briefing_v1
  - `trusted_tree = 0.947453`
  - `residual_chord = 0.458141`
  - `anchor_qualified = 0.472929`
  - `plain = 0.965303`
- archive_v1
  - `trusted_tree = 0.964907`
  - `residual_chord = 0.420230`
  - `anchor_qualified = 0.478995`
  - `plain = 0.981380`

So this memo earns the narrow machine-side sentence:

- the current Gate12A structural replay survives across the full current Llama family set

## 3. First-Pass Family Surface On Llama

The attached local provisional first-pass captures are:

- transcript_v1
  - `high_tension: surface_noise_only = 4; phantom_barrier = 2`
  - `flat: conflict_respected = 6`
- briefing_v1
  - `high_tension: conflict_respected = 6`
  - `flat: support_fused = 6`
- archive_v1
  - `high_tension: conflict_adopted_break_candidate = 6`
  - `flat: surface_noise_only = 4; conflict_respected = 2`

This means the current Llama family set does not collapse to one phenotype rule.
It preserves distinct family-conditioned first-pass behavior under one fixed model.

## 4. Comparison Against Existing Lines

### 4.1 What matched Qwen

The current Llama transcript_v1 first-pass split matched the current Qwen transcript_v1 split exactly:

- `high_tension: surface_noise_only = 4; phantom_barrier = 2`
- `flat: conflict_respected = 6`

The current Llama archive_v1 first-pass split also matched the current Qwen archive_v1 split exactly:

- `high_tension: conflict_adopted_break_candidate = 6`
- `flat: surface_noise_only = 4; conflict_respected = 2`

### 4.2 What did not match Qwen

The current Llama briefing_v1 first-pass split did not match the current Qwen briefing_v1 split.

Current Qwen briefing_v1 split:

- `high_tension: support_fused = 4; conflict_respected = 2`
- `flat: conflict_respected = 2; support_fused = 2; surface_noise_only = 2`

Current Llama briefing_v1 split:

- `high_tension: conflict_respected = 6`
- `flat: support_fused = 6`

So the current Llama family-set completion supports:

- structural portability across the full fixed family set
- transcript-level phenotype agreement with Qwen
- archive-level phenotype agreement with Qwen

but not:

- full family-set phenotype agreement with Qwen

### 4.3 What this does to the SmolLM2 read

The current SmolLM2 transcript_v1 fallback line still differs at the flat band:

- `flat: hypothetical_rejection = 6`

So the present state is:

- transcript_v1 matched Qwen on Llama
- archive_v1 matched Qwen on Llama
- briefing_v1 diverged on Llama
- SmolLM2 already diverged at transcript flat

This is enough to say:

- phenotype portability is neither model-free nor family-free under the current evidence base

## 5. What This Memo Earns

This memo earns only the following narrow sentence:

- `meta-llama/Llama-3.2-1B-Instruct closes the current Gate12A family set at machine scope; transcript_v1 and archive_v1 reproduce the current Qwen-like first-pass splits, while briefing_v1 remains family-conditioned at first pass.`

This is enough to say:

- one additional dense transformer closes the current family set structurally
- transcript and archive phenotype behavior each reproduced the current Qwen family read on Llama
- briefing remained the family-conditioned instability point within the current Llama set

This is not enough to say:

- that all dense transformers now share one common phenotype law
- that briefing_v1 has stabilized across models
- that `202` should be replaced as the published checkpoint
- that a Zenodo-bound release tag is now warranted

## 6. Release Boundary Note

This memo should be treated as:

- a tracked post-checkpoint follow-up memo

and not as:

- a new release tag boundary

The current published checkpoint remains `202`.
The memo still depends on local provisional first-pass attachments under `runs/`.
So it belongs in:

- git history

but not yet in:

- a new Zenodo-triggering release tag
