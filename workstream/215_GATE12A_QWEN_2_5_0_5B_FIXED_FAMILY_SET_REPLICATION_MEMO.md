# Gate12A Qwen2.5-0.5B Fixed Family Set Replication Memo

Status: empirical model memo draft
Role: scoped lower-bound dense-transformer family-set empirical memo over the current frozen Gate12A observable surface, recording one completed `Qwen/Qwen2.5-0.5B` fixed-family-set replay plus one memo-local first-pass read of the generated family packets, not a universal law claim, not a checkpoint rewrite, and not a Gate12B promotion
Date: 2026-04-19

This memo proceeds from:

- `195_GATE11_ADMISSIBILITY_MEMBRANE_FREEZE.md`
- `196_GATE12_DISCRETE_CONNECTION_CONSTITUTION.md`
- `197_GATE12A_DISCRETE_CONNECTION_IMPLEMENTATION_CONTRACT.md`
- `198_GATE12A_DISCRETE_CONNECTION_AUDIT.md`
- `200_GATE12A_TRANSCRIPT_V1_ANCHOR_RICH_CLOSURE_TENSION_REPLICATION_MEMO.md`
- `201_GATE12A_BRIEFING_V1_ANCHOR_RICH_CLOSURE_TENSION_EMPIRICAL_MEMO.md`
- `202_GATE12A_TRANSCRIPT_V1_VS_BRIEFING_V1_ANCHOR_RICH_CLOSURE_TENSION_COMPARISON_MEMO.md`
- `203_GATE12A_ARCHIVE_V1_ANCHOR_RICH_CLOSURE_TENSION_FIRST_PASS_BREAK_CANDIDATE_MEMO.md`
- `210_GATE12A_QWEN_2_5_3B_INSTRUCT_FIXED_FAMILY_SET_REPLICATION_MEMO.md`
- `211_GATE12A_LLAMA_3_2_3B_INSTRUCT_FIXED_FAMILY_SET_REPLICATION_MEMO.md`
- `212_GATE12A_QWEN3_4B_FIXED_FAMILY_SET_REPLICATION_MEMO.md`

## 0. Summary

This memo records the completed `Qwen/Qwen2.5-0.5B` fixed-family-set replay under the current frozen Gate12A observable surface.

The main result is narrow:

- `Qwen/Qwen2.5-0.5B` closes the current `transcript_v1 / briefing_v1 / archive_v1` family set at machine scope
- this adds a lower-bound dense-transformer family-set closure at 0.5B under the same frozen Gate12A surface
- the machine-side structural checks stay live across all three families
- the first-pass family readings remain descriptive packet reads only

This is tracked post-checkpoint memory.
It does not move the published checkpoint boundary beyond `202`.

## 1. Scope

This memo asks only:

- `what stayed fixed, and what shifted, when Qwen/Qwen2.5-0.5B was taken through the current transcript_v1 / briefing_v1 / archive_v1 family set under the unchanged Gate12A observable surface?`

Its subject is fixed to:

- `Qwen/Qwen2.5-0.5B`
- `transcript_v1 / briefing_v1 / archive_v1`
- `current committed Gate12A cross-model replay harness`
- `current generated 12-row reading packets`

It does:

- keep the Gate12A observable unchanged
- keep the model fixed to one dense-transformer line
- close the current `Qwen2.5-0.5B` family set at machine scope
- record one memo-local first-pass read for each generated family packet
- treat the line as a lower-bound dense-transformer family-set completion

It does not:

- claim a transformer-universal law
- claim that all phenotype behavior is model-invariant
- claim scale monotonicity inside Qwen
- promote a new threshold doctrine
- revise the published checkpoint boundary beyond `202`
- add a Zenodo or release claim

## 2. Evidence Base

This memo is limited to one current local run surface:

- `runs/gate12a_cross_model_replay_qwen_qwen2_5_0_5b/`
- `runs/gate8cm_qwen_qwen2_5_0_5b_transcript_128r_candidate_execution/`
- `runs/gate8cm_qwen_qwen2_5_0_5b_briefing_200r_candidate_execution/`
- `runs/gate8cm_qwen_qwen2_5_0_5b_archive_128r_candidate_execution/`
- `runs/gate12a_triangle_reading_packet_balanced_recheck_from_gate12a_upstream_gate8cm_qwen_qwen2_5_0_5b_transcript_128r_gate9k/`
- `runs/gate12a_triangle_reading_packet_balanced_recheck_from_gate12a_upstream_gate8cm_qwen_qwen2_5_0_5b_briefing_200r_gate9k/`
- `runs/gate12a_triangle_reading_packet_balanced_recheck_from_gate12a_upstream_gate8cm_qwen_qwen2_5_0_5b_archive_128r_gate9k/`

The current cross-model replay summary records:

- `run_id = gate12a_cross_model_replay_qwen_qwen2_5_0_5b`
- `code_git_commit = 0e9d5d101775c943a45f3955f5d7c7342b22bf11`
- `family_set = transcript_v1 / briefing_v1 / archive_v1`
- `summarize_only = true`

Each generated reading packet has:

- `packet_row_count = 12`
- `high_tension_packet_count = 6`
- `flat_packet_count = 6`
- `selection_mode = balanced_per_band`

So this memo concerns:

- one completed `Qwen/Qwen2.5-0.5B` fixed family set under the current committed protocol
- one memo-local first-pass read of the three generated balanced packets

and not:

- a new machine-side observable
- a new reviewed `runs/` first-pass artifact
- a release-bound checkpoint revision

## 3. Structural Replay Held Across The Full Qwen2.5-0.5B Family Set

Across all three current `Qwen/Qwen2.5-0.5B` families, the same machine-side structural checkpoints stayed live:

- `zero_overlap_clear = True`
- `all_defined_triangles_anchor_rich = True`
- `trusted_tree_gt_residual_chord = True`
- `plain_gt_anchor_qualified = True`

The current medians are:

- transcript_v1
  - `trusted_tree = 1.024616`
  - `residual_chord = 0.868073`
  - `anchor_qualified = 0.856132`
  - `plain = 1.040963`
- briefing_v1
  - `trusted_tree = 1.016194`
  - `residual_chord = 0.864395`
  - `anchor_qualified = 0.785857`
  - `plain = 1.032405`
- archive_v1
  - `trusted_tree = 1.017569`
  - `residual_chord = 0.924433`
  - `anchor_qualified = 0.830989`
  - `plain = 1.026520`

So this memo earns the narrow machine-side sentence:

- the current Gate12A structural replay survives across the full current `Qwen/Qwen2.5-0.5B` family set

This matters because the current dense-transformer family-set surface now has a lower-bound closure at 0.5B under the same frozen Gate12A observable.

## 4. First-Pass Family Surface On Qwen2.5-0.5B

The memo-local first-pass readings of the generated balanced packets are:

- transcript_v1
  - `high_tension: surface_noise_only = 6`
  - `flat: conflict_respected = 4; compensatory_closure = 2`
- briefing_v1
  - `high_tension: support_fused = 2; anchor_overreach = 2; surface_noise_only = 2`
  - `flat: surface_noise_only = 4; compensatory_closure = 2`
- archive_v1
  - `high_tension: conflict_adopted_break_candidate = 6`
  - `flat: surface_noise_only = 4; conflict_respected = 2`

These are packet reads only.
They are not promoted to a family-wide phenotype law.

### 4.1 Transcript_V1 Read

The transcript high-tension packet is surface-noise-only at this first pass:

- all six high-tension rows preserve the supported relation
- the answer text treats transcript drift or cleanup as non-dispositive
- no high-tension row in this packet adopts a conflict anchor or blocks the supported closure

The transcript flat packet is mixed:

- four rows keep the supported closure and explicitly refuse a cross-exam aside
- two rows withhold the fused ancestor conclusion under an explicit no-fusion instruction

The two closure-withholding flat rows are recorded as `compensatory_closure` rather than as a strong phenotype claim.
They should remain a cautious first-pass read because the answer follows an explicit no-fusion instruction rather than merely rejecting support without context.

### 4.2 Briefing_V1 Read

The briefing high-tension packet is mixed:

- two rows wrongly fuse separate packets into one bridge under an explicit no-fusion warning
- two rows follow a counter-brief against the aligned packet pair
- two rows preserve the supported relation with only surface-level presentation drift

The briefing flat packet is also mixed:

- four rows preserve the supported relation and treat presentation variation as non-dispositive
- two rows refuse to fuse split briefing packets under an explicit no-fusion warning

So the current briefing read does not collapse to one phenotype rule even within the 0.5B model.

### 4.3 Archive_V1 Read

The archive high-tension packet is the sharpest break-bearing surface in this 0.5B family set:

- all six high-tension rows follow the disputed memo instead of the aligned archive chain
- all six are therefore recorded as `conflict_adopted_break_candidate`

The archive flat packet is milder:

- four rows preserve the supported archive closure while treating surface variation as non-dispositive
- two rows keep the aligned archive closure and explicitly refuse the disputed memo

So the archive read separates cleanly by band in this packet:

- high tension is fully break-bearing at first pass
- flat remains support-preserving at first pass

## 5. What This Memo Earns

This memo earns only the following narrow sentence:

- `Qwen/Qwen2.5-0.5B closes the current Gate12A family set at machine scope as a lower-bound dense-transformer completion; its first-pass packet read is surface-noise-only at transcript high tension, mixed in briefing, and fully conflict-adopted at archive high tension, while flat packets remain mostly support-preserving with two cautious compensatory-closure reads in transcript and briefing.`

This is enough to say:

- one 0.5B Qwen line closes the current family set structurally
- the current dense-transformer family-set evidence now includes a lower-bound 0.5B closure under the frozen Gate12A surface
- the structural layer remains separable from the first-pass phenotype layer
- the archive high-tension packet is break-bearing in this lower-bound line

This is not enough to say:

- that all dense transformers now share one common phenotype law
- that Qwen phenotype behavior is scale-monotone
- that the previous 3B/4B reads should be rewritten
- that the current transcript_v1 or briefing_v1 phenotype surface has stabilized
- that `202` should be replaced as the published checkpoint
- that a Zenodo-bound release tag is now warranted

## 6. Release Boundary Note

This memo should be treated as:

- a tracked post-checkpoint follow-up memo

and not as:

- a checkpoint rewrite
- a new release tag boundary
- a new Zenodo claim

The current published checkpoint remains `202`.
The memo depends on local generated packet reads and belongs in:

- git history

but not yet in:

- a new Zenodo-triggering release tag

## 7. Next Honest Move

The next honest move is:

1. keep the `Qwen/Qwen2.5-0.5B` result as lower-bound family-set post-checkpoint memory
2. keep the machine-side structural sentence separate from the first-pass packet tags
3. do not rewrite `213` in this patch
4. later consider adding one narrow sentence to `213` that the dense-transformer family-set surface now has a 0.5B lower-bound closure under the same frozen Gate12A observable

The line should remain:

- no checkpoint rewrite
- no Gate12B promotion
- no threshold doctrine
- no release claim
