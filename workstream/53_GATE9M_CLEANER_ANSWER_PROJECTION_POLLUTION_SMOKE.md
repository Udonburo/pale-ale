# Gate9M Cleaner Answer-Projection Pollution Smoke

Status: tracked smoke execution snapshot
Role: tracked Gate9M cleaner-side pollution-anatomy read, not blocker resolution or operator opening
Date: 2026-03-21

This first tracked Gate9M smoke read executes the pollution-anatomy audit defined in:

- `52_GATE9M_CLEANER_ANSWER_PROJECTION_POLLUTION.md`

The next cleaner-side role-coupling spec is now recorded in:

- `54_GATE9N_CLEANER_ANSWER_PROJECTION_ROLE_COUPLING.md`

The first tracked Gate9N smoke execution read is now recorded in:

- `55_GATE9N_CLEANER_ANSWER_PROJECTION_ROLE_COUPLING_SMOKE.md`

## 0. Scope

This file records the first committed-code smoke execution of the Gate9M cleaner-side `answer_projection` pollution audit.

It is not:

- a blocker-resolution claim
- a trusted-tree bypass success
- an operator-opening event
- a role-policy rewrite

It is:

- a tracked handoff for the first cleaner-side pollution-anatomy audit
- a code-bound read on whether cleaner-side `answer_projection` pollution is structural return-leg residue, policy-mixing residue, or both
- the current scientific judgment on what remains named after the first split

The tracked evidence package is:

- `runs/gate9m_cleaner_answer_projection_pollution_smoke_from_gate9l/manifest.json`
- `runs/gate9m_cleaner_answer_projection_pollution_smoke_from_gate9l/cleaner_answer_projection_pollution_registry.jsonl`
- `runs/gate9m_cleaner_answer_projection_pollution_smoke_from_gate9l/cleaner_answer_projection_policy_compare.csv`
- `runs/gate9m_cleaner_answer_projection_pollution_smoke_from_gate9l/cleaner_answer_projection_pollution_status.json`
- `runs/gate9m_cleaner_answer_projection_pollution_smoke_from_gate9l/gate9m_cleaner_answer_projection_pollution_read.md`

## 1. Source And Bind

This smoke run consumes the first Gate9L residual-pollution bundle:

- `source_gate9l_run_id = gate9l_first_tree_answer_projection_pollution_smoke_from_gate9k`
- `source_gate9l_code_git_commit = e71bfcc0071f0d506186714e9a69132062032c36`

It reuses the recovered Gate9A graph:

- `source_gate9a_dir = runs/gate9f_conflict_anchor_recovery_smoke_from_gate9e/gate9a_recovered_from_gate9f`

The Gate9M audit bind is:

- `method_id = gate9m_cleaner_answer_projection_pollution_audit_v1`
- `code_git_commit = a4bce3b8f3bca368af361cedfb720c1e32337b03`

## 2. What Landed

Gate9M does not suppress `answer_projection`.

It only asks:

- whether cleaner-side `answer_projection` pollution remains because it is structurally carrying the declared return leg
- whether the same edge family is also being mixed into the residual verdict role in a way that is avoidable without law change

The public split stays explicit:

- `structural_return_leg_pollution`
- `policy_mixing_pollution`

## 3. Smoke Read

### 3.1 Cleaner Residual Pollution Is Real At Baseline

The baseline cleaner-side counts are:

- `baseline_cleaner_residual_answer_projection_edge_count = 8`
- `cleaner_support_cycle_answer_projection_edge_count = 4`
- `cleaner_conflict_cycle_answer_projection_edge_count = 0`

So cleaner-side residual pollution is not hypothetical.

It is tied to support-cycle participation, not conflict-cycle participation.

### 3.2 Structural Return-Leg Pollution Is Triggered

The first half of the split lands as:

- `structural_return_leg_pollution_status = triggered`
- `removing_cleaner_answer_projection_breaks_closure_doctrine_status = triggered`

That is the correct read for this bundle.

Cleaner-side `answer_projection` is not just decorative residue.

It is carrying declared closure work on the cleaner support-cycle side.

### 3.3 Policy-Mixing Pollution Is Also Triggered

The second half of the split also lands:

- `policy_mixing_pollution_status = triggered`
- `split_policy_cleaner_residual_answer_projection_edge_count = 0`
- `split_policy_conflict_residual_answer_projection_edge_count = 8`
- `split_policy_conflict_bridge_preservation_status = clear`

This matters because the bundle does not force cleaner-side `answer_projection` to remain in the residual verdict role once the role split is declared.

Under the audit split:

- cleaner-side `answer_projection` can move into `closure_return_leg_auxiliary`
- conflict-side residual bridge availability still remains clear

So the blocker is not pure structural inevitability.

It is structural closure duty plus residual-role coupling.

### 3.4 The Next Blocker Is Role Coupling

The final Gate9M status is:

- `suppression_requires_scalar_masking_status = denied`
- `undeclared_role_surgery_required_status = denied`
- `next_named_blocker = cleaner_answer_projection_role_coupling`

This is the correct landing point.

Gate9M does not say cleaner-side `answer_projection` can simply be removed.

It says the current cleaner-side pollution survives because one declared edge family is doing two jobs at once:

- closure return-leg duty
- residual verdict-role duty

## 4. Current Scientific Judgment

The correct Gate9M smoke judgment is:

- Gate9M succeeded as a pollution-anatomy audit
- cleaner-side `answer_projection` pollution is genuinely structural at the closure-doctrine level
- the same edge family is also genuinely policy-mixed into the residual verdict role
- conflict-side bridge preservation survives the audit split
- the next honest named blocker is therefore `cleaner_answer_projection_role_coupling`

The strongest honest sentence is:

- `Gate9M shows that cleaner-side answer-projection pollution is neither purely removable residue nor purely unavoidable structure: it is structural return-leg work coupled to a residual verdict role, and that coupling is now the next named blocker.`

## 5. What This Smoke Run Earns

This smoke run earns the right to say:

- the cleaner-side pollution anatomy is no longer vague
- the blocker has narrowed from residual pollution in general to role coupling in particular
- future work can test declared role splits without pretending the issue is still undifferentiated

## 6. What This Smoke Run Does Not Earn

This smoke run does not earn the right to say:

- the blocker is resolved
- cleaner-side `answer_projection` can already be suppressed in the mainline
- trusted-tree bypass now succeeds
- operator admission should open

## 7. Next Honest Move

The next honest move is not:

- declare success on bypass
- open operator admission
- collapse the issue back into scalar dominance rhetoric

The next honest move is:

- attack `cleaner_answer_projection_role_coupling` directly as the next named blocker
