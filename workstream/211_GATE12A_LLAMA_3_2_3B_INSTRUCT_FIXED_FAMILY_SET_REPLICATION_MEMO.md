# Gate12A Llama-3.2-3B-Instruct Fixed Family Set Replication Memo

Status: empirical model memo draft
Role: scoped dense-transformer family-set empirical memo over the current Gate12A observable surface, recording one completed `meta-llama/Llama-3.2-3B-Instruct` fixed-family-set replay plus attached local provisional first-pass reads, not a universal law claim, not a checkpoint rewrite, and not a Gate12B promotion
Date: 2026-04-04

This memo proceeds from:

- `195_GATE11_ADMISSIBILITY_MEMBRANE_FREEZE.md`
- `196_GATE12_DISCRETE_CONNECTION_CONSTITUTION.md`
- `197_GATE12A_DISCRETE_CONNECTION_IMPLEMENTATION_CONTRACT.md`
- `198_GATE12A_DISCRETE_CONNECTION_AUDIT.md`
- `200_GATE12A_TRANSCRIPT_V1_ANCHOR_RICH_CLOSURE_TENSION_REPLICATION_MEMO.md`
- `201_GATE12A_BRIEFING_V1_ANCHOR_RICH_CLOSURE_TENSION_EMPIRICAL_MEMO.md`
- `202_GATE12A_TRANSCRIPT_V1_VS_BRIEFING_V1_ANCHOR_RICH_CLOSURE_TENSION_COMPARISON_MEMO.md`
- `203_GATE12A_ARCHIVE_V1_ANCHOR_RICH_CLOSURE_TENSION_FIRST_PASS_BREAK_CANDIDATE_MEMO.md`
- `206_GATE12A_LLAMA_3_2_1B_INSTRUCT_FIXED_FAMILY_SET_REPLICATION_MEMO.md`
- `210_GATE12A_QWEN_2_5_3B_INSTRUCT_FIXED_FAMILY_SET_REPLICATION_MEMO.md`

## 0. Scope

This memo asks only:

- `what stayed fixed, and what shifted, when meta-llama/Llama-3.2-3B-Instruct was taken through the current transcript_v1 / briefing_v1 / archive_v1 family set under the unchanged Gate12A observable surface?`

Its subject is fixed to:

- `meta-llama/Llama-3.2-3B-Instruct`
- `transcript_v1 / briefing_v1 / archive_v1`
- `current committed Gate12A cross-model replay harness`
- `current committed first-pass capture layer`

It does:

- keep the Gate12A observable unchanged
- keep the model fixed to one dense-transformer line
- close the current `Llama-3.2-3B-Instruct` family set at machine scope
- attach one local provisional first-pass read to each family packet
- compare the resulting first-pass family set against the current `Llama-3.2-1B-Instruct` and `Qwen/Qwen2.5-3B-Instruct` lines

It does not:

- claim a transformer-universal law
- claim that all phenotype behavior is now model-invariant
- promote a new threshold doctrine
- revise the published checkpoint boundary beyond `202`

## 1. Evidence Base

This memo is limited to one current local run surface:

- `runs/gate12a_cross_model_replay_meta_llama_llama_3_2_3b_instruct/`
- `runs/gate8cm_meta_llama_llama_3_2_3b_instruct_transcript_128r_candidate_execution/`
- `runs/gate8cm_meta_llama_llama_3_2_3b_instruct_briefing_200r_candidate_execution/`
- `runs/gate8cm_meta_llama_llama_3_2_3b_instruct_archive_128r_candidate_execution/`
- `runs/gate12a_triangle_phenotype_first_pass_recheck_from_gate12a_upstream_gate8cm_meta_llama_llama_3_2_3b_instruct_transcript_128r_gate9k/`
- `runs/gate12a_triangle_phenotype_first_pass_recheck_from_gate12a_upstream_gate8cm_meta_llama_llama_3_2_3b_instruct_briefing_200r_gate9k/`
- `runs/gate12a_triangle_phenotype_first_pass_recheck_from_gate12a_upstream_gate8cm_meta_llama_llama_3_2_3b_instruct_archive_128r_gate9k/`

The current cross-model replay summary records:

- `run_id = gate12a_cross_model_replay_meta_llama_llama_3_2_3b_instruct`
- `code_git_commit = 084eb7878d8cb016243950e1cf4b4bd7379daaba`
- `family_set = transcript_v1 / briefing_v1 / archive_v1`
- `summarize_only = true`

So this memo concerns:

- one completed `Llama-3.2-3B-Instruct` fixed family set under the current committed protocol

and not:

- an arbitrary cross-model sweep

## 2. Structural Replay Held Across The Full Llama-3.2-3B Family Set

Across all three current `Llama-3.2-3B-Instruct` families, the same machine-side structural checkpoints stayed live:

- `zero_overlap_clear = True`
- `all_defined_triangles_anchor_rich = True`
- `trusted_tree_gt_residual_chord = True`
- `plain_gt_anchor_qualified = True`

The current medians are:

- transcript_v1
  - `trusted_tree = 0.984596`
  - `residual_chord = 0.656484`
  - `anchor_qualified = 0.600202`
  - `plain = 0.992019`
- briefing_v1
  - `trusted_tree = 0.974122`
  - `residual_chord = 0.599501`
  - `anchor_qualified = 0.622937`
  - `plain = 0.989118`
- archive_v1
  - `trusted_tree = 0.974740`
  - `residual_chord = 0.573509`
  - `anchor_qualified = 0.606336`
  - `plain = 0.986051`

So this memo earns the narrow machine-side sentence:

- the current Gate12A structural replay survives across the full current `Llama-3.2-3B-Instruct` family set

## 3. First-Pass Family Surface On Llama-3.2-3B

The attached local provisional first-pass captures are:

- transcript_v1
  - `high_tension: phantom_barrier = 4; surface_noise_only = 2`
  - `flat: compensatory_closure = 2; conflict_respected = 2; surface_noise_only = 2`
- briefing_v1
  - `high_tension: anchor_overreach = 2; compensatory_closure = 2; support_fused = 2`
  - `flat: surface_noise_only = 6`
- archive_v1
  - `high_tension: conflict_adopted_break_candidate = 4; surface_noise_only = 2`
  - `flat: surface_noise_only = 6`

This means the current `Llama-3.2-3B-Instruct` family set does not collapse to one phenotype rule.
It preserves distinct family-conditioned first-pass behavior under one fixed model.

## 4. Comparison Against Existing Lines

### 4.1 Transcript no longer reproduces the smaller Llama or Qwen transcript line

The current `Llama-3.2-3B-Instruct` transcript split does not match the current smaller-Llama or `Qwen/Qwen2.5-3B-Instruct` transcript split:

- current `Llama-3.2-3B-Instruct` transcript
  - `high_tension: phantom_barrier = 4; surface_noise_only = 2`
  - `flat: compensatory_closure = 2; conflict_respected = 2; surface_noise_only = 2`
- current `Llama-3.2-1B-Instruct` transcript
  - `high_tension: surface_noise_only = 4; phantom_barrier = 2`
  - `flat: conflict_respected = 6`
- current `Qwen/Qwen2.5-3B-Instruct` transcript
  - `high_tension: conflict_respected = 4; hypothetical_rejection = 2`
  - `flat: conflict_respected = 4; surface_noise_only = 2`

So the transcript line stays structurally portable while remaining scale-sensitive even inside the current Llama lane.

### 4.2 Briefing loses the smaller-Llama family shape

The current `Llama-3.2-3B-Instruct` briefing line does not reproduce the smaller-Llama family split:

- current `Llama-3.2-3B-Instruct` briefing
  - `high_tension: anchor_overreach = 2; compensatory_closure = 2; support_fused = 2`
  - `flat: surface_noise_only = 6`
- current `Llama-3.2-1B-Instruct` briefing
  - `high_tension: conflict_respected = 6`
  - `flat: support_fused = 6`

It also does not match the current `Qwen/Qwen2.5-3B-Instruct` briefing line:

- current `Qwen/Qwen2.5-3B-Instruct` briefing
  - `high_tension: support_fused = 4; conflict_respected = 2`
  - `flat: anchor_overreach = 2; conflict_respected = 2; surface_noise_only = 2`

So the current `Llama-3.2-3B-Instruct` briefing line is neither a straight continuation of the smaller Llama line nor a Qwen-like replay.

### 4.3 Archive keeps the break-bearing high-tension pressure but washes the flat band to surface-only

The current `Llama-3.2-3B-Instruct` archive line stays archive-conditioned, but its flat band is simpler than either comparison line:

- current `Llama-3.2-3B-Instruct` archive
  - `high_tension: conflict_adopted_break_candidate = 4; surface_noise_only = 2`
  - `flat: surface_noise_only = 6`
- current `Llama-3.2-1B-Instruct` archive
  - `high_tension: conflict_adopted_break_candidate = 6`
  - `flat: surface_noise_only = 4; conflict_respected = 2`
- current `Qwen/Qwen2.5-3B-Instruct` archive
  - `high_tension: conflict_adopted_break_candidate = 4; surface_noise_only = 2`
  - `flat: compensatory_closure = 2; conflict_respected = 2; surface_noise_only = 2`

So the current `Llama-3.2-3B-Instruct` archive line keeps the break-bearing high-tension pressure while collapsing the flat packet to a pure surface-noise read.

## 5. What This Memo Earns

This memo earns only the following narrow sentence:

- `meta-llama/Llama-3.2-3B-Instruct closes the current Gate12A family set at machine scope, but its first-pass family surface no longer reproduces the smaller Llama line: transcript becomes phantom-barrier dominant with a compensatory flat minority, briefing disperses across mixed high-tension tags while flat collapses to surface_noise_only, and archive keeps break-bearing high tension while washing the flat band to surface_noise_only.`

This is enough to say:

- one larger Llama line closes the current family set structurally
- transcript, briefing, and archive all remain family-conditioned at first pass
- phenotype portability stays narrower than machine-side structural portability

This is not enough to say:

- that all dense transformers now share one common phenotype law
- that the current Llama line has stabilized across scale
- that archive_v1 now has one fixed flat-band sentence
- that `202` should be replaced as the published checkpoint
- that a Zenodo-bound release tag is now warranted

## 6. Release Boundary Note

This memo should be treated as:

- a tracked post-checkpoint follow-up memo

and not as:

- a new release tag boundary

The current published checkpoint remains `202`.
The memo still depends on local provisional first-pass attachments under `runs/`.
So it belongs in:

- git history

but not yet in:

- a new Zenodo-triggering release tag
