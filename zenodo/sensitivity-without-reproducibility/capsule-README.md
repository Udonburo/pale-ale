# Sensitivity Without Reproducibility — Reproducibility Capsule

This is a model-forward-free public capsule for the technical report
*Sensitivity Without Reproducibility*.

It contains:

- buildable report and figure sources;
- the primary-source citation audit;
- all 60 continuous Stage M layer-block records in CSV and JSON;
- the public Stage M data manifest and README;
- the source-hash-locked Python exporter;
- exact checkpoint/runtime bindings;
- the licensing split; and
- an internal SHA-256 inventory.

The exporter requires the immutable Stage M source whose SHA-256 is embedded
in the script. That private execution artifact is not redistributed. Running
the exporter or building the report performs no model execution, activation
collection, or inferential analysis.

Stage E remained unopened. The capsule contains no behavioral outcome for the
planned representation--function analysis and no post-hoc rescue result.
