# Gate8C Second Rendering Family

Status: second-family implementation complete, full burn executed
Role: post-Gate8B second portability spec, not bridge spec
Date: 2026-03-21

## 0. Why This Exists

Gate8A established:

- `standing strong, bridge unresolved`

Gate8B first pass established:

- `gate7c revival` survived one controlled rendering-family shift under `briefing_v1`

So the next question is narrower than explanation and still narrower than generalization in the large.

The Gate8C question is:

- does `gate7c revival` survive a second controlled rendering-family shift

This is still a portability question.

It is not yet:

- a bridge-rescue question
- a field question
- a candidate-promotion question

## 1. Primary Question

The only primary question of Gate8C is:

- `gate7c revival survives second regime shift?`

The regime shift in this workstream must come from:

- exactly one new rendering family

## 2. Court Freeze

The standing court remains fully frozen.

The following are fixed:

- candidates: `F`, `gate6f`, `gate6h`, `gate7c`
- standing evaluator
- label provenance rules
- mixed-granularity caveat
- quietness pairing rule
- aggregation ban
- diagnostic-only bridge status

The following remain forbidden:

- adding a new ranking candidate
- changing evaluator logic
- changing aggregation rules
- mixing bridge outputs into standing
- introducing classifier, fusion, or rescue layers

## 3. Truth And Taxonomy Freeze

The second family must preserve:

- `world truth`
- the four Gate8 cells
- `answer_target_type`
- quietness pairing by shared `world_id`

It may change only:

- rhetorical packaging
- retrieval voice
- chunk ordering
- prompt framing
- answer phrasing

It may not change:

- truth construction
- cell semantics
- quietness control semantics
- bridge burden

## 4. New Family Requirement

The second family must be materially distinct from both:

- `archive_v1`
- `briefing_v1`

This means not merely:

- renaming packets as notes
- swapping one heading for another

It should instead introduce a genuinely different rhetorical surface, while leaving the underlying truth and taxonomy untouched.

## 5. Falsifiers

Gate8C weakens or fails under any of the following:

- `gate7c <= F` on either conflict cell
- quietness survives only by losing the conflict-side revival
- conflict-side revival survives only by losing quietness
- the second family produces instability large enough that the Gate8B portability sentence no longer looks like a court-level fact

More sharply:

- if `gate7c` loses direct contradiction or distributed incompatibility against `F`, the portability claim fails on this second shift
- if `gate7c` wins conflict only by collapsing the quietness side, the result is mixed rather than clean

## 6. What Stays Out

Gate8C is not allowed to become:

- bridge v4
- benchmark broadening
- conflict-family expansion
- field aggregation
- transport-motif comparison

Those remain separate workstreams.

## 7. Non-Promotion Rule

Even if Gate8C survives, it still does not earn:

- candidate promotion
- replacement of `F`
- explanation credit
- a claim of general regime invariance

At most, it earns:

- the right to say that the current `gate7c revival` survives two controlled rendering-family shifts under a frozen court

## 8. Implementation Gate

Implementation is allowed only if:

- the new addition is one rendering family only
- `world truth`, taxonomy, `answer_target_type`, and quietness pairing are preserved exactly
- falsifiers are written first
- the court freeze remains explicit

If implementation lands, the next burden is not theory.

It is:

- execute the second-family burn honestly and read the court without rescue operations

## 9. Implementation And Full Burn

That implementation has now landed with:

- `rendering_family_id = transcript_v1`

The full second-family execution runs are:

- `runs/gate8q_128r_transcript_constitution`
- `runs/gate8q_128r_transcript_benchmark`
- `runs/gate8q_128r_transcript_candidate_execution`
- `runs/gate8r_200r_transcript_constitution`
- `runs/gate8r_200r_transcript_benchmark`
- `runs/gate8r_200r_transcript_candidate_execution`

These runs bind:

- `code_git_commit = 744bc7a`

## 10. Current Read

The second-family read is not a clean portability success.

It is a useful boundary read.

At both 128 rows and 200 rows:

- `gate7c` remains ahead of `F` on direct contradiction
- `gate7c` remains ahead of `F` on distributed incompatibility
- `F` is quieter on `mean_delta_p90`
- `F` is quieter on `mean_top10_inflation`

So the active Gate8C sentence is:

- the conflict-side revival survives the second regime shift, but the quietness side collapses there

That means the Gate8C result is:

- mixed
- boundary-finding
- not a clean second portability win

It still does not license:

- bridge rescue
- field aggregation
- candidate promotion
- replacement of `F`
