# Graph-XOR R1 v1.0 — public specification excerpt

This is a public-safe excerpt of the frozen R1 v1.0 scientific specification.
The canonical repository file is bound by SHA-256:

```text
fa6afd7502da3d4edd348fb626b9585344ee276cb8b3cef3d5b059e143a27e87
```

## Frozen question

> Do frozen pretrained transformers make an externally defined graph-XOR
> obstruction class linearly accessible during single-pass processing,
> transform it naturally under realized gauge and cycle-coordinate changes,
> and causally use it across held-out graph and semantic families?

## Mathematical object

For the marked-theta construction, let the three marked path parities be

```text
Q = (q1, q2, q3) in F2^3.
```

The obstruction coordinates in the marked cycle basis are

```text
C = (c1, c2) = (q1 + q3, q2 + q3) in F2^2.
```

The abstraction chain is

```text
edge cochain b -> path parities Q -> obstruction coordinates C -> queried answer alpha^T C.
```

The map from `Q` to `C` has kernel `span{(1,1,1)}`. `Q` is a refinement of
`C`; `C` is a quotient. A common-mode coordinate such as `g = q3` is
gauge-covariant, not a new gauge-invariant obstruction.

## Identifiability boundary

- High intervention interchange accuracy for `C` would qualify `C` as a
  valid causal abstraction relative to the frozen intervention family and
  evaluation distribution.
- It would not establish a unique internal ontology, information compression,
  minimality, or the absence of finer representations such as `Q` or `b`.
- Decodability without the frozen causal intervention would not qualify a
  causal carrier.
- Behavioral failure would not establish absolute absence of hidden-state
  information.

## Capability dependency

The hidden-state study was conditional on a behavioral capability gate.
R1 v1.0 required a frozen model configuration to pass selection and untouched
confirmation before activation extraction, alignment search, naturality, or
causal intervention could open.

## Final R1 v1.0 state

```text
CLOSED_AT_C0
S0_SELECTION_FAIL
ACTIVATION_NOT_AUTHORIZED
ALIGNMENT_NOT_AUTHORIZED
SEMANTIC_HOLDOUT_CLOSED
```

No predeclared Qwen3-0.6B or Qwen3-1.7B configuration qualified for capability
confirmation under the frozen zero-shot, official non-thinking, symbolic-XOR
S0 protocol. R1.1 and R1.2 were separate prospective scouts, not amendments to
this result.
