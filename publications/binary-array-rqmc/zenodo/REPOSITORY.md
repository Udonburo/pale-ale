# Repository and reproduction identity

The development repository is https://github.com/Udonburo/pale-ale.

This deposit does not claim that an existing Git commit or GitHub release
contains the package. The exact reproduction inputs are the files in the
version-specific Zenodo capsule identified by doi:10.5281/zenodo.22728405,
with the included SHA-256 inventory.

Inside the capsule, main.md, build_pdf.py, pdf-style.typ, figures/, and repro/
are sufficient for the documented builds and checks after installing the
listed dependencies. Source-repository-relative paths in repro/data/provenance.json
are lineage labels, not downloadable dependencies or required machine paths.
They do not imply that omitted raw experimental data are included.

The compact archive preserves the stated reproduction scope. No private
workstream directory, credentials, runtime cache, or model weights are included.
