# Gate9N Cleaner Answer-Projection Role Coupling

Status: spec-only, implementation not yet landed
Role: Gate9N cleaner-side role-coupling spec, not blocker resolution or operator opening
Date: 2026-03-21

Gate9N proceeds from:

- `52_GATE9M_CLEANER_ANSWER_PROJECTION_POLLUTION.md`
- `53_GATE9M_CLEANER_ANSWER_PROJECTION_POLLUTION_SMOKE.md`

## 0. Why This Exists

Gate9M narrowed the blocker sharply.

What is now known is:

- cleaner-side `answer_projection` pollution is structurally real
- cleaner-side `answer_projection` pollution is also policy-mixed into the residual verdict role
- conflict-side bridge preservation remains clear under the audit split
- the next named blocker is `cleaner_answer_projection_role_coupling`

So the next honest move is not:

- operator opening
- tree-choice sensitivity
- a bypass success claim
- a doctrine rewrite

It is:

- auditing whether the cleaner-side role coupling can be separated declaratively under the frozen law

## 1. Scope

Gate9N studies only:

- cleaner-side `answer_projection` role coupling
- the declared split between `closure_return_leg_auxiliary` and `residual_chord_candidate`
- whether that split can be carried without degrading conflict-side bridge preservation or breaking closure doctrine

It does not:

- change the trusted-edge policy
- change the first forest
- change the cycle family
- reopen operator admission
- introduce new metrics or new public roles

## 2. Fixed Conditions

Gate9N keeps all of the following fixed:

- trusted-edge policy fixed
- first forest fixed
- closure doctrine fixed
- operator admission still denied

This matters because Gate9N is not allowed to solve role coupling by silently changing the doctrine that created the coupling.

## 3. Public Question

The Gate9N question is:

- `can cleaner-side answer_projection role coupling be separated declaratively without degrading conflict-side bridge preservation or breaking closure doctrine?`

This question is intentionally narrow.

It does not ask whether the final mainline should already adopt the split.

It asks whether the coupling is separable at audit level under the existing doctrine.

## 4. Public Object

Gate9N must emit:

- a role-coupling registry
- a policy-compare table
- a deterministic status payload
- a short tracked readout

The public object is not a new score.

It is:

- a role-coupling separation audit

## 5. Source Run

The frozen source run for Gate9N is:

- `runs/gate9m_cleaner_answer_projection_pollution_smoke_from_gate9l`

If that source run is unavailable or inconsistent with the current spec, implementation must stop at gap reporting.

## 6. Expected Output Files

Gate9N implementation should emit exactly these first-class outputs:

- `manifest.json`
- `cleaner_answer_projection_role_coupling_registry.jsonl`
- `cleaner_answer_projection_role_coupling_policy_compare.csv`
- `cleaner_answer_projection_role_coupling_status.json`
- `gate9n_cleaner_answer_projection_role_coupling_read.md`

Additional helper files are allowed only if they do not change the public object.

## 7. Required Status Keys

The Gate9N status payload must emit at least:

- `baseline_cleaner_residual_answer_projection_edge_count`
- `declared_split_cleaner_auxiliary_answer_projection_edge_count`
- `declared_split_conflict_residual_answer_projection_edge_count`
- `conflict_bridge_preservation_status`
- `closure_doctrine_preservation_status`
- `cleaner_pollution_reduction_status`
- `role_coupling_separability_status`
- `scalar_masking_violation_status`
- `undeclared_role_surgery_required_status`
- `next_named_blocker`

These keys are required so the implementation remains comparable to Gate9M and does not drift into prose-only judgment.

## 8. Falsifiers

Gate9N must keep these falsifiers explicit:

- conflict-side bridge degrades
- closure doctrine breaks
- cleaner-side pollution remains unchanged after the declared split
- suppression requires scalar masking or undeclared role surgery

If those falsifiers land, the role-coupling line has not earned separability.

## 9. Forbidden

The Gate9N implementation must not:

- rename blockers
- rename falsifiers
- widen scope
- introduce new metrics
- introduce new public roles
- change doctrine
- promote operator admission

If the spec appears insufficient, implementation must:

- emit the gap clearly
- stop without filling it by invention

## 10. What This Spec Can Earn

At most, Gate9N can earn the right to say:

- cleaner-side `answer_projection` role coupling is or is not declaratively separable under fixed doctrine

It still does not earn:

- blocker resolution
- trusted-tree success
- operator opening
- a new geometry claim

## 11. Opus-Safe Delegation Boundary

Gate9N is safe to delegate only as:

- narrow consumer implementation
- unit tests
- status payload emission
- tracked smoke handoff formatting

Gate9N is not delegated as:

- blocker naming
- falsifier design
- scope narrowing
- doctrine change
- next workstream judgment

## 12. Current Memory Hook

The shortest acceptable sentence is:

- test whether cleaner answer-projection role coupling is separable before trying to suppress it
